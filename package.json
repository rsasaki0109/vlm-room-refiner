{
  "name": "vlm-room-refiner",
  "private": true,
  "description": "API(8010) + Next(3000) 同時起動。Ollama は別途。",
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "bash scripts/run-api.sh",
    "dev:web": "cd frontend && npm run dev",
    "screenshot": "node scripts/screenshot-ui.mjs",
    "dogfood": "bash backend/run-dogfood.sh"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "concurrently": "^9.1.0"
  }
}
